import * as d from '../../../declarations';
export declare function transformToNativeComponentText(compilerCtx: d.CompilerCtx, buildCtx: d.BuildCtx, cmp: d.ComponentCompilerMeta, inputJsText: string): string;
